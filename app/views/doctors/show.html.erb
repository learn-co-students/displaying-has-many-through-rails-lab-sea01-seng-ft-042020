<h1><%= @doctor.name %></h1>
<h3>Department: <%= @doctor.department %></h3>
<p>Appointments:</p>
<% @doctor.appointments.each do |appointment| %>
    <ul><%= link_to appointment.patient.name, patient_path(@doctor_patient) %></ul>
    <ul><%= appointment.readable_format %></ul><br>
<% end %>
